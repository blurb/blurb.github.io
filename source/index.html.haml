---
alias: blog/
title: Blurb Engineering Blog
---
%section#main
  .row
    .columns.small-12
      .articles
        - blog.articles.each do |article|
          %article
            %h2
              %a{:href => article.url}
                = article.title
            %h3
              = "By #{article.data.author}"
            %time
              = article.date.strftime('%b %e %Y')
            = article.body
            %h4
              = "Tagged: #{article.tags.join(", ")}"

