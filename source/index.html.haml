---
alias: blog/
title: Blurb Engineering Blog
---
%section#main
  .row
    .columns.small-12
      .articles
        - blog.articles[0...5].each do |article|
          %h2
            %a{:href => article.url}
              = article.title
          %time
            = article.date.strftime('%b %e %Y')
          = article.summary
          %a{:href => article.url}
            Read more &raquo;

