---
title: Recent Posts
layout: blog
---
.articles
  - blog.articles[0...5].each do |article|
    %article
      %h3
        %a{:href => article.url}
          = article.title
        %time
          = article.date.strftime('%b %e %Y')
      = article.summary
      %a{:href => article.url}
        Read more